-- Copyright (C) 1984, 1985  by Xerox Corporation. All rights reserved. -- File: DiagDiskTime.mesa ---- DIRECTORY PhysicalVolume		USING	[ID], System			USING	[LocalTimeParameters]; DiagDiskTime: DEFINITIONS = BEGIN-- module function ---- This interface allows the real time clock to be set without touching the disk, even if a time server is not available.-- Note:  The pvIDs passed to the LTP procedures must be already asserted.  This is to insure that the physical volume is healthy. -- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  -- SIGNALS --  RealTimeClockFailure:	ERROR;  -- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  -- TYPES --  LocalTime:		TYPE = RECORD [				year:	[0..2104],				month:	[0..12),				day:	[0..31],				hour:	[0..24),				minute:	[0..60),				second:	[0..60),				weekday:[0..6],				dst: 	BOOLEAN];  -- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  -- PROCEDURES --  GetLocalTime:		PROCEDURE [time: STRING --31 characters min--];  -- returns a string with the local time   GetLocalTimeFromUser:	PROCEDURE RETURNS [lt: LocalTime];   GetLTPFromUser:	PROCEDURE RETURNS [ltp: System.LocalTimeParameters];   SetLTP:		PROCEDURE [pvID: PhysicalVolume.ID]; -- to write the Local Time Parameters on the disk -- will return the SIGNAL RealTimeClockFailure with a Real Time Clock hardware failure   SetRealTimeClock:	PROCEDURE [			   displayMsg: BOOLEAN¬ FALSE,			   timeFromServerOnly: BOOLEAN¬ FALSE]; -- to set the hardware Real Time Clock -- will return the SIGNAL RealTimeClockFailure with a Real Time Clock hardware failure    -- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~     END...  Edited:   