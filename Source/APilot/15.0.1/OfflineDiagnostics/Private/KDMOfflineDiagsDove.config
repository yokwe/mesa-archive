-- File: KDMOfflineDiagsDove.config-- Last edited on  13-Jan-86 13:17:24  by  KL---- Copyright (C) 1985, 1986 by Xerox Corporation. All rights reserved.---- Purpose: Used to make KDMDove.boot, which contains the mouse, keyboard, --	    beeper and display diagnostics for the Dove machines. --	    This boot files has the American keyboard selected. To create boot --	    files containing other keyboards (European or Japanese), --	    KDMMsgesAndKBDsImplDove.mesa needs to be recompiled with the following--	    BOOLEANs set appropriately:----		AmericanKeyboard: BOOLEAN--		EuropeanKeyboard: BOOLEAN--		JapaneseKeyboard: BOOLEAN----	    Only one of the three BOOLEANs may be set to TRUE. Once recompiled, --	    the system can be rebuilt by doing a Bind and MakeBoot.---- To make a boot file, do the following:----      Makeboot KDMOfflineDiagsDove [--       	 parm: UtilityPilot.bootmesa, --		 parm: DebugUtilityPilot.bootmesa]/dhu--DIRECTORY  UtilityPilotClientImpl: FROM "OfflineDiagnosticControlModule",  PilotKernel: FROM "UtilityPilotKernel",  HeapImpl: FROM "UnpackedHeapImpl";  PACK TimeImpl, HeapImpl, FormatImpl, StringsImplA, StringsImplB, TTYImpl,     -- OfflineDiagKernelDove modules     UtilityPilotClientImpl,     OfflineDiagTTYImplDove,     CMDiagMsgKeysImplDove,          -- Implementation modules specific to KDMDove.boot     KDMMsgesAndKBDsImplDove,     KDMOfflineDiagnosticsDove;      KDMOfflineDiagsDove: CONFIGURATION  IMPORTS  KernelPrograms, RuntimePrograms, CommunicationPrograms,           RuntimeInternal   EXPORTS  ALL =	     BEGIN    -- Implementation modules.    OfflineDiagTTYImplDove;  -- TTY for offline diagnostics     UtilityPilotClientImpl;  -- OfflineDiagnosticControlModule    CMDiagMsgKeysImplDove;  -- OfflineDiagnosticControlModule messages        BasicHeadsDove; PilotKernel; TimeImpl; HeapImpl; StringsImplA;    StringsImplB; TTYImpl; FormatImpl;        -- Implementation modules specific to KDMOfflineDiagnosticsDove    KDMMsgesAndKBDsImplDove; -- KDMOfflineDiagnosticsDove messages and keyboards    KDMOfflineDiagnosticsDove; -- Diagnostic implementations    END... KDMOfflineDiagnosticsDove.config       LOGCreated on 13-Jun-85  KL