-- Copyright (C) 1984, 1985  by Xerox Corporation. All rights reserved. -- File: DiagDiskTimerImpl.mesa--  DIRECTORY  Process		USING	[SetTimeout, Ticks],  DiagDiskTimer		USING	[];  DiagDiskTimerImpl: MONITOR  IMPORTS	Process  EXPORTS	DiagDiskTimer = BEGIN  resetTimer: BOOLEAN¬ FALSE;  timerCondition: CONDITION; -- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -- to abort the timer -- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  ResetTimer: PUBLIC ENTRY PROCEDURE  = BEGIN  resetTimer¬ TRUE;  NOTIFY timerCondition;  END; -- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ -- to start the timer -- ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~  StartTimer: PUBLIC ENTRY PROCEDURE [ticks: Process.Ticks]   	RETURNS [timedOut: BOOLEAN¬ FALSE]  = BEGIN  resetTimer¬ FALSE;  Process.SetTimeout[condition: @timerCondition, ticks: ticks];  IF NOT resetTimer THEN WAIT timerCondition;  IF NOT resetTimer THEN timedOut¬ TRUE;  END; --StartTimer  END....--of  DiagDiskTimerImpl.mesa