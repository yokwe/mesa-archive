-- Copyright (C) 1984, 1985  by Xerox Corporation. All rights reserved. -- PilotSwitchesExtra5.mesa,  5-Apr-85 14:31:51 by ETDIRECTORY  PilotSwitches USING [PilotDomainC];<< This module defines even more booting switches used by Pilot which need to be   folded into PilotSwitches >>   PilotSwitchesExtra5: DEFINITIONS =   BEGIN    -- Control of Communications with respect to the IEEE 802.3 standard  ieeeLevelZeroPacketFormat: PilotSwitches.PilotDomainC = '\361;  bilingualReception: PilotSwitches.PilotDomainC = '\362;  bilingualTranmission: PilotSwitches.PilotDomainC = '\363;    remoteCallDebugger: PilotSwitches.PilotDomainC = '\364;    -- Simulate 640k bytes of Dove memory    tinyDoveMemSizeTinyVMTinyDisplay: PilotSwitches.PilotDomainC = '\352;  -- Simulate 640k bytes dove memory with 64 page VM Map, 15" display  tinyDoveMemSizeTinyVMBigDisplay:PilotSwitches.PilotDomainC = '\353;  -- Simulate 640k bytes dove memory with 64 page VM Map, 19" display  tinyDoveMemSizeMedVMTinyDisplay: PilotSwitches.PilotDomainC = '\354;  -- Simulate 640k bytes dove memory with 128 page VM Map, 15" display  tinyDoveMemSizeMedVMBigDisplay: PilotSwitches.PilotDomainC = '\355;  -- Simulate 640k bytes dove memory with 128 page VM Map, 19" display  tinyDoveMemSizeBigVMTinyDisplay:PilotSwitches.PilotDomainC = '\356;  -- Simulate 640k bytes dove memory with 256 page VM Map, 15" display  tinyDoveMemSizeBigVMBigDisplay: PilotSwitches.PilotDomainC = '\357;  -- Simulate 640k bytes dove memory with 256 page VM Map, 19" display    END.