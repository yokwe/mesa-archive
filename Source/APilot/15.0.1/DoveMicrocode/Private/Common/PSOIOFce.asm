;Copyright (C) 1985 by Xerox Corporation. All rights reserved.;-- file PSOIOFce.asm;-- PSO IOFace;-- create on  9-Sep-85 15:05:29;-- Last edited by:;--	JMA		 5-Dec-85 15:35:59	:;=============================  PSO IO FACE  =================================;PSOIOFCE.asm 	- contains defs for the IORegion;-- The IORegion is used as shared memory;--    for Parallel Port Head and PSO Handler communications.;--    See IORPSO.ASM for the complete structure;-----------------------------  Definitions  --------------------------------------------; Bitwise definitions;Command Register  -- Head down to HandlerPSOUpdateStatCmd	EQU	0001H	;Update Status, NowPSOResetCmd		EQU	0002H	;Peform a board resetPSOTransferCmd		EQU	0004H	;Transfer modePSODiagCmd		EQU	0008H	;Place board on maintenance modeCmdPending		EQU	0FF00H	;Set command pending flag;Transfer ModeOutPutBlock		EQU	00H	;Perform a block output, use DMAOutPutByte		EQU	01H	;Perform a byte output, use DMA;InputByte		EQU	02H	;Perform a byte input, use DMA;InputBlock		EQU	03H	;Perform a block input, use DMA;Port Status Register -- Parallel Port StatusFaultBit		EQU	01H	;Fault line indicator (active low)SelectBit		EQU	02H	;Select lineBusySyncBit		EQU	04H	;Busy lineAcknowledgeBit		EQU	08H	;Acknowledge line (active low);Command StatusNotReady		EQU	00H	;Ready			EQU	01H	;InProgress		EQU	02H	;Incomplete		EQU	03H	;Exceptional		EQU	04H	;for PROM Error and others(?)