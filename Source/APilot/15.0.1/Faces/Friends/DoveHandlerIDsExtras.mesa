-- File: DoveHandlerIDsExtras.mesa - last edit:-- Bowers.PA            6-Feb-87  9:59:00-- DoveHandlerIDsExtras.mesa  -- Copyright (C) 1987 by Xerox Corporation. All rights reserved.DIRECTORY  DoveInputOutput USING [HandlerID],  DoveHandlerIDs USING [parity];DoveHandlerIDsExtras: DEFINITIONS SHARES DoveInputOutput =  BEGIN  << NOTE: This module contains only constants, and should not be included by any     definitions module.  Thus it will always be possible to recompile it without     recompiling all the implementation modules including it. >>  << RAM-based handlers: these constants may be altered or extended.     However, all bootloaded code (incl. IOP code) must use the same values. >>  pceSerial: DoveInputOutput.HandlerID = SUCC[DoveHandlerIDs.parity];  cmosDisplay: DoveInputOutput.HandlerID = SUCC[pceSerial];  pceClock: DoveInputOutput.HandlerID = SUCC[cmosDisplay];  END.LOG 6-Feb-87  9:56:52  -  FHB -- Created for Pilot12.4b release 23-Oct-86 17:22:00 -- FHB -- Added pceSerial and pceClock