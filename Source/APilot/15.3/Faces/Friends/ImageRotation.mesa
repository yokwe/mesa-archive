--File: ImageRotation.mesa--Last Edit: Monahan, 18-Dec-86 15:17:54--Copyright (C) 1986 by Xerox Corporation.  All rights reserved.DIRECTORY  ESCAlphaExtras USING [aROTATEBITS],  Mopcodes USING [zESC];ImageRotation: DEFINITIONS =  BEGIN« RotateBits operates on a 16x16 block of bits, word-aligned.  For both source and destination, the words-per-line parameter may be either positive (so that the pointer addresses the first word of the block) or negative (so that the pointer addresses the last word of the block).  The resulting matrix of rotation functions is as follows:	srcWPL > 0, dstWPL > 0:	90-degree rotation clockwise	srcWPL > 0, dstWPL < 0: mirroring around diagonal from top right to bottom left	srcWPL < 0, dstWPL > 0: mirroring around diagonal from top left to bottom right	srcWPL < 0, dstWPL < 0: 90-degree rotation counterclockwise »-- ********************************************************--Procedures MicroCode-- ********************************************************RotateBits: PROCEDURE [srcPtr: LONG POINTER, srcWPL: INTEGER, dstPtr: LONG POINTER, dstWPL: INTEGER] = MACHINE CODE  {Mopcodes.zESC, ESCAlphaExtras.aROTATEBITS};--Procedures Software-- ********************************************************SoftwareRotateBits: PROCEDURE [srcPtr: LONG POINTER, srcWPL: INTEGER, dstPtr: LONG POINTER, dstWPL: INTEGER];END.LOG3-Oct-86 -- Monahan -- Created.16-Oct-86 -- Monahan -- Renamed and added *RotateGray PROCs.18-Dec-86 -- Monahan -- Removed *RotateGray PROCs.