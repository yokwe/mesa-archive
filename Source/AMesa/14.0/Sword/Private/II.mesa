-- File: II.mesa - last edit:-- Litman              24-May-87 17:13:59-- Copyright (C) 1987 by Xerox Corporation. All rights reserved.DIRECTORYIT USING [LG, Handle];II: DEFINITIONS = {  -- cross references the current selection in the flow and config subwindows  CrossReference: PROC[h: IT.Handle] RETURNS[foundSelection: BOOLEAN];    -- clears out the black marks in the flow and config subwindows  ClearMarks: PROC[h: IT.Handle];    -- if selection is config or global frame, prints out the date.  DateForSelection: PROC[h: IT.Handle];    -- if selection is global frame, fetches the bcd.  FetchSelection: PROC[h: IT.Handle];    -- if selection is in the process sw, returns a local frame.  -- if selection is in the config sw, returns a global frame.  -- if selection is the name of a module, returns a global frame.  -- else returns a nil frame.  FrameFromSelection: PROC[h: IT.Handle] RETURNS[frame: LONG POINTER, lg: IT.LG];  }.