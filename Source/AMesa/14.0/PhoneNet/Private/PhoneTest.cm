--PhoneTest.cm  - (last edited by AOF 25-Nov-85 14:59:00)//compiling the testCompiler /-a-bej-np-u PhoneTestImpl PhoneTestToolImplBinder PhoneTest PhoneTestTool//running a testRun RS232CIOHeadsDLion RS232CIO PhoneNetConfig PhoneTest//PhoneTest registers a series of executive procs////  p.Init <mode><duplexity>//  This initialized the level-0 phone driver//    <mode> is either 'a => active or 'p => passive//    <duplexity> is either 'f => full or 'h => half////  p.Adopt//  Adds NS prtocol family support to the phonenet driver////  p.Disown//  Undoes affect of p.adopt////  p.Destroy//  Undoes the affect of p.init//  p.send <address>/n //  Sends 'n' packets to <address> (AddressTranslation conformance).////  p.Receive//  Readies receiver so that it may be the target of "p.send"////  p.Stop//  Undoes the affect of p.receive//  p.EchoUser <target>/<count>//  Sends <count> incrementing length/pattern packets to <target> with//  expectation that they will be echoed.  Packets are checked for checksum//  and content when they are returned.  The output will be 'O or 'E for every//  successfully echoed packet that is odd or even length respectively.  Packets//  that are echoed in error will have an '* appened to the 'O | 'E.//    //  p.EchoServer//  The echo server must be run the machine that is the <target> of the //  p.EchoUser command.////  p.EchoStop//  Stops the echo server////  p.TestLoop//  Loops through starting and stopping the PhoneNetDriver////  p.Stats//  Prints out PhoneNetFriends statistics////  p.Verbose <mode>//  Set | Resets verbose mode of packet error reporting.  Verbose mode dumps//  the entire packet in one of several formats selectable via the <mode>//  switch.  Available modes are//    'o => 8-bit octal display//    'x => 8-bit hex display//    'a => ASCII 